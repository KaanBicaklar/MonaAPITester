# postman2burp
A Burp Suite extension that imports Postman .json collections directly into Burp. It parses requests (method, URL, headers, body) and displays them in a table, allowing users to send selected requests to Repeater.
